<br>
<h3 class="text-center">@<%= @user.username %>'s Followers</h3>
<br>

<%= render "user_list" %>

<div class="text-center">
	<%= link_to "Feed", feed_path %> |
	<%= link_to "@#{@user.username}'s Profile", show_user_path(id: @user.id) %>
</div>